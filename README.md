Product Warehouse Management System
โปรเจกต์นี้เป็นระบบจัดการสินค้าคงคลังเบื้องต้น (เพิ่ม, ลบ, ดู) ที่พัฒนาขึ้นสำหรับแบบทดสอบทางเทคนิค

หมายเหตุ: โปรเจกต์นี้พัฒนาขึ้นในเวลาจำกัดโดยผู้พัฒนาที่ไม่มีประสบการณ์การเขียน C# .NET และ Vue.js มาก่อน จึงได้มีการใช้งาน AI (Gemini) เป็นผู้ช่วยในการเขียนโค้ด (Pair-Programming) เป็นส่วนใหญ่ เพื่อเรียนรู้และสร้างโครงสร้างพื้นฐานของโปรแกรม รวมถึงช่วยในการแก้ปัญหาต่างๆ

🛠️ Tech Stack (เทคโนโลยีที่ใช้)
Backend: C# (.NET 8), ASP.NET Core Web API

Frontend: Vue.js 3 (ใช้ TypeScript)

Database: PostgreSQL

State Management: Pinia

Routing: Vue Router

Authentication: JWT (JSON Web Tokens)

UI: Bootstrap 5

✨ Features (ความสามารถ)
Authentication: ระบบสมัครสมาชิก (Register) และ ล็อกอิน (Login) ด้วย JWT

Data Tenancy: ผู้ใช้แต่ละคนจะเห็นและจัดการได้เฉพาะสินค้าที่ตัวเองเพิ่มเข้าไปเท่านั้น

Protected Routes: หน้าจัดการสินค้าจะเข้าถึงได้เฉพาะผู้ที่ล็อกอินแล้ว

Product CRUD:

Create: เพิ่มรหัสสินค้า (ตรวจสอบความถูกต้อง 16 หลัก A-Z, 0-9)

Read: แสดงรายการสินค้า พร้อม Barcode

Delete: ลบสินค้า (พร้อมกล่องยืนยัน)

Barcode: สร้าง Barcode มาตรฐาน CODE39 จากรหัสสินค้า

Validation: มีการตรวจสอบความถูกต้องของข้อมูลทั้งฝั่ง Frontend และ Backend

🚀 Getting Started (วิธีรันโปรเจกต์)
คุณจะต้องรันทั้ง Backend และ Frontend แยกกัน

1. Backend (โฟลเดอร์ ProductApi)
ตั้งค่า Database:

เปิดไฟล์ appsettings.json

แก้ไข ConnectionStrings.PostgresConnection ให้ตรงกับฐานข้อมูล PostgreSQL ของคุณ

คืนค่า Dependencies:

Bash

dotnet restore
รันโปรเจกต์:

Bash

dotnet run
API Server จะเริ่มทำงาน (เช่น http://localhost:5090)

โปรแกรมจะสร้างตารางและ User ทดสอบ (admin@test.com / Password123!) ให้ในฐานข้อมูลโดยอัตโนมัติ

2. Frontend (โฟลเดอร์ product-ui)
ตั้งค่า API URL:

เปิดไฟล์ src/services/api.ts

แก้ไขตัวแปร baseURL ให้ตรงกับ URL ของ Backend ที่รันอยู่

ติดตั้ง Dependencies:

Bash

npm install
รันโปรเจกต์ (Development Mode):

Bash

npm run dev
Frontend จะเริ่มทำงาน (เช่น http://localhost:5173)

เปิดเบราว์เซอร์แล้วไปที่ URL ดังกล่าวเพื่อใช้งาน